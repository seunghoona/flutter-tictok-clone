import 'package:flutter/material.dart';

class PostViewButton extends StatelessWidget {
  const PostViewButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: const BoxDecoration(color: Colors.red),
    );
  }
}
